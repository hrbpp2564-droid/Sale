name: Deploy to Vercel

# Vercel auto-deploys via Git Integration on push to main.
# This workflow is a placeholder — actual deployment is handled by Vercel.
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment handled by Vercel
        run: echo "Vercel Git Integration deploys automatically on push to main."
